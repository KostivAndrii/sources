[Unit]
Description=limnoria irc bot
After=network.target

[Service]
Type=simple
User={{ limnoria_user }}
ExecStart=/usr/bin/supybot {{ limnoria_config }}
Restart=always
WorkingDirectory={{ limnoria_dir }}

[Install]
WantedBy=multi-user.target
